{
  "name": "kanban",
  "version": "1.0.0",
  "description": "Sistema Kanban com tema dark/light",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/styles.css -o ./dist/output.css --watch",
    "build": "npx tailwindcss -i ./src/styles.css -o ./dist/output.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["kanban", "tailwind", "dark-mode"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.16"
  }
}
